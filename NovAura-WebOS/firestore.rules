rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit any logged in user to read or write any document
    // This stops Firebase from blocking user uploads/saves
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
