rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permit any logged in user to read or write any file
    // This stops Firebase from blocking user uploads/saves
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
